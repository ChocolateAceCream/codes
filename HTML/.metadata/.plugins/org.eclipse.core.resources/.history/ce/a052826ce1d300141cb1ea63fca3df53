@pi=Math::PI


class Area
      def self.new(shape)
        if shape=='Circle'
            puts "input r"
            r=gets.chomp.to_f
            area=r*r*Math::PI
            puts "area of circle is #{area}"
            
        end
      end
end


Area.new('Circle')
