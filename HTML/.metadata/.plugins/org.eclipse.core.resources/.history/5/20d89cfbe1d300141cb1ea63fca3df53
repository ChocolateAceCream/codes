@pi=Math::PI

class Area
      def self.new(shape)
        if shape=='Circle'
            puts "input r"
            r=18.2
            area=r*r*Math::PI
            puts "area of circle is #{area}"          
        end
        
        if shape=='R=14.8'
        end
      end
end

Area.new('Circle')
Area.new('R=14.8'')
