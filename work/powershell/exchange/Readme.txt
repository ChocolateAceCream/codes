1. open exchange management shell from start menu
2. Go to the directory where both scripts stored.

To generate report for database: 
Enter ./report-db.ps1 to run the first script. A cvs fill will be generated under the same directory where the script stored

To generate report for all users:
enter ./report-email.ps1 -all